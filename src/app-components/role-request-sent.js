import React from 'react';

const RoleRequestSentMessage = ({ className }) => (
  <small className={`has-text-weight-light ${className}`}>Your access request has been sent to the Admin for approval. You will receive an email once the request is approved.</small>
);

export default RoleRequestSentMessage;
