import React from 'react';
import SkipMainContent from './components/SkipMainContent';
import './header.scss';

const Header = () => (
  <header aria-label='Site header'>
    <SkipMainContent />
  </header>
);

export default Header;
